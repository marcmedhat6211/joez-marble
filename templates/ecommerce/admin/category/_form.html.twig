{{ form_start(form, {'attr':{"data-toggle":"validator", 'novalidate':'novalidate'}}) }}
<div class="row">
    <div class="col-12 col-md-6 mb-3">
        <div class="form-group">
            {{ form_label(form.title) }}
            {{ form_widget(form.title) }}
            {{ form_errors(form.title) }}
        </div>
    </div>

    <div class="col-12 col-md-6 mb-3">
        <div class="form-group">
            <div class="media mt-0">
                {% if category.image %}
                    <div class="mr-3">
                        <a href="{{ absolute_url(asset(category.image.absolutePath)) }}" data-popup="lightbox">
                            <img src="{{ absolute_url(asset(category.image.absolutePath)) }}" width="60" height="60" class="rounded-pill" alt="{{ category.client }}">
                        </a>
                    </div>
                {% endif %}

                <div class="media-body">
                    <div class="custom-file">
                        {{ form_label(form.headerImageOne) }}
                        {{ form_widget(form.headerImageOne) }}
                        {{ form_errors(form.headerImageOne) }}
                    </div>
                    <span class="form-text text-muted">Size: 165 px x 165 px</span>
                </div>
            </div>
        </div>
    </div>

    <div class="col-12 col-md-6">
        <div class="custom-control custom-switch custom-switch-square custom-control-success mb-2">
            {{ form_widget(form.living) }}
            {{ form_label(form.living) }}
            {{ form_errors(form.living) }}
        </div>
    </div>
</div>
<div class="text-right">
    <button type="submit" class="btn btn-primary">Submit form <i class="icon-paperplane ml-2"></i></button>
</div>
{{ form_end(form) }}